import { DocumentItem, TranscriptEntry } from '../types'

// Empty initial documents array - user uploads real audio files
export const INITIAL_DOCUMENTS: DocumentItem[] = []

export const DEFAULT_TRANSCRIPT: TranscriptEntry[] = []
